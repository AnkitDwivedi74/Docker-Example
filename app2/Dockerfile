# Use a base image with a web server (e.g., nginx)
FROM nginx:alpine

# Copy the application files to the container
COPY . /usr/share/nginx/html

# Specify any additional configurations or commands (if needed)

